# Graph Engineering

Graph-based representation of project knowledge, dependencies, contracts and relationships.

The graph complements Markdown documentation; it does not silently replace the canonical documents.
